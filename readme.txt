README file for codeml_FE

This folder contains the source files and makefile for compiling the modified version of codeml used in Le et al 2006 .....
To create codeml_FE download the program folder, cd to the program folder and type 
make -f Makefile
This will create the executable file called codeml_FE

Luke:
NOTE  for 64 bit computers use    make -f Makefile.UNIX

